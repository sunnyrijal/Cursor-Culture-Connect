# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=culture_connect
DB_USER=postgres
DB_PASSWORD=postgres

# JWT configuration
JWT_SECRET=culture_connect_secret_key_change_in_production

# Server configuration
PORT=5002

# Database seeding
SEED_DATABASE=true

# Node environment
NODE_ENV=development
